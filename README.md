# GpuMemoryAllocator

C# bindings for d3d12ma and vma, based on silk.net.

## Build

- Note that you need to clone the submodule.

- Windows
  - Required clang
  - Required .NET 10
  ```powershell
  ./init.ps1
  ./build.ps1
  ```
